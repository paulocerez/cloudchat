import React from 'react';
import { View } from 'react-native';

export default function SpacerScreen() {
  // This screen is intentionally empty - it's just a spacer in the tab bar
  return <View style={{ flex: 1 }} />;
}
